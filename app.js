let gitCli = 'programmer moh';
console.log(gitCli);

const name = document.getElementById('root', function () {
    const now = 'btn';
    const programmer = now.padStart(length.toString(programmer))
})
